/**
 * Central error -> HTTP status mapping.
 * IllegalTransitionError -> 409, ValidationError -> 400,
 * NotFoundError/CrossVenueAccessError -> 404, everything else -> 500 (logged).
 * PHASE 5 STUB.
 */
export {};
