/**
 * Typed client for the mock Paygate provider (POST /charges, POST /refunds),
 * including Idempotency-Key handling and retry-safe behavior for the 10%
 * transient 500s. PHASE 6 STUB.
 */
export {};
