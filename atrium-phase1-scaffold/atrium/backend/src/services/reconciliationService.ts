/**
 * INV-5: every captured charge maps to exactly one CONFIRMED booking or
 * exactly one refund. Exposes the reconciliation report/endpoint that must
 * return zero discrepancies. PHASE 6 STUB.
 */
export {};
